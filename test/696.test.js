/* eslint-disable no-undef */

import countBinerySubstrings from '../code/696'

// 测试用例（test..expect..toBe 为固定写法）
test('revertByWorld:Let\'s take LeetCode contest', () => {
    expect(countBinerySubstrings("00110011")).toBe(6)
})

